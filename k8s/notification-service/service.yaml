apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: constructflow
spec:
  selector:
    app: notification-service
  ports:
    - port: 50052
      targetPort: 50052
  type: ClusterIP
